## 1. 动作模型

- [x] 1.1 新增清理动作类型、可用性和能力的 TypeScript 类型。
- [x] 1.2 实现从健康问题类型和建议动作到动作能力的确定性映射。
- [x] 1.3 为当前 analyzer 可能产生的每种健康问题类型和建议组合增加单元测试。

## 2. 清理计划集成

- [x] 2.1 为每个清理计划项附加动作能力元数据。
- [x] 2.2 保留当前候选目标标签逻辑，但在把它视为可操作前必须检查动作能力。
- [x] 2.3 确保仅观察和仅人工判断的项目永远不会被标记为可执行。

## 3. UI 与导出

- [x] 3.1 在清理计划卡片中展示紧凑的动作能力 badge。
- [x] 3.2 更新中文和英文文案，覆盖可执行、仅预览、仅观察和仅人工判断状态。
- [x] 3.3 更新 Markdown 导出，包含动作类型、可用性、风险等级和目标标签要求。
- [x] 3.4 仅对可执行计划项展示应用/回退按钮。
- [x] 3.5 如果计划项包含 AI 辅助信息，UI 和导出中必须区分本地动作能力与 AI 辅助建议。

## 4. AI 增强分析边界

- [x] 4.1 定义 AI 辅助字段，例如说明、优先级提示、候选目标标签和风险提示。
- [x] 4.2 确保 AI 输出不能修改 action availability 或绕过本地动作矩阵。
- [x] 4.3 增加测试覆盖：AI 建议不能把 `observeOnly` 或 `manualReview` 提升为可执行能力。

## 5. 文档

- [x] 5.1 更新 roadmap，将动作能力建模标记为 0.2 的下一个安全增量。
- [x] 5.2 如果 UI 文案改变了清理计划描述，同步更新 README 当前限制。
- [x] 5.3 在当前 change 中明确：`executable` 必须同时具备预览、操作日志和撤销要求。
- [x] 5.4 记录 AI 增强分析只能作为辅助建议，不能决定可执行性。

## 6. 验证

- [x] 6.1 运行 `npm test`。
- [x] 6.2 运行 `npm run build`。
- [x] 6.3 运行 `npm run spec:validate -- define-cleanup-action-capabilities`。
- [x] 6.4 使用 `npm run local:install-dev` 本地安装，并确认测试库插件目录包含最新产物。

## 7. 可执行动作

- [x] 7.1 将确定性合并/重命名动作映射为 `executable`。
- [x] 7.2 实现按文件预览写入 frontmatter tags 的受限变更逻辑。
- [x] 7.3 应用清理动作时写入操作日志。
- [x] 7.4 为最近一次清理操作提供回退能力，并在 frontmatter 已变化时阻止覆盖。
