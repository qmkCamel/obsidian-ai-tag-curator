## 1. 视图模型

- [x] 1.1 新增健康报告 UI view model 类型，区分总览、AI 状态、行动项和规则证据。
- [x] 1.2 实现从 `TagHealthReport`、`CleanupPlan`、可选 `TagHealthAiAnalysis` 生成行动项的纯函数。
- [x] 1.3 增加单元测试：AI 未运行时不生成优先处理项，只返回初始态。
- [x] 1.4 增加单元测试：AI 已运行时，priority 通过 issue type 和 tags 匹配 cleanup item，并继承本地 action capability。
- [x] 1.5 增加单元测试：AI 未匹配到 cleanup item 时不得生成可执行行动项。

## 2. 健康报告弹窗结构

- [x] 2.1 重构 `TagHealthReportModal` 的渲染结构为总览层、AI 行动层、规则证据层。
- [x] 2.2 初始态展示空状态说明和“生成 AI 辅助分析”按钮。
- [x] 2.3 分析中态展示非阻塞 loading、阶段提示和可继续查看规则证据的说明。
- [x] 2.4 结果态展示 AI 聚合后的优先处理项。
- [x] 2.5 规则证据层改为六类 tab 或 segmented control，每次突出一个分类。
- [x] 2.6 保留现有标签点击搜索能力。
- [x] 2.7 保留可执行项的应用/回退能力，并确保不可执行项不展示应用按钮。

## 3. AI 与规则文案

- [x] 3.1 在中文和英文 labels 中补充规则证据层、AI 行动层、初始态和分析中态文案。
- [x] 3.2 文案中明确“规则是证据层，AI 是行动建议层”。
- [x] 3.3 文案中明确 AI 不改变动作可执行性。
- [x] 3.4 移除或降级旧的平行 AI 报告文案，避免 AI 分析和规则分析重复铺开。

## 4. 样式

- [x] 4.1 按设计稿增加总览统计区样式。
- [x] 4.2 增加 AI 初始态空面板样式。
- [x] 4.3 增加 AI 分析中面板和阶段状态样式。
- [x] 4.4 增加优先处理项卡片样式，复用现有标签 pill、优先级、置信度和动作能力 badge。
- [x] 4.5 增加规则证据 tab 和紧凑证据列表样式。
- [x] 4.6 检查深色主题下文本不重叠、按钮不挤压、宽窄 modal 下布局仍可读。

## 5. 导出与复制

- [x] 5.1 更新 Markdown 复制内容，先输出 AI 优先处理项，再输出规则证据摘要。
- [x] 5.2 AI 未运行时，复制内容只输出规则证据摘要和“AI 未运行”的说明。
- [x] 5.3 可执行项导出仍包含动作能力、目标标签、文件预览和风险提示。

## 6. 文档

- [x] 6.1 更新 README 截图占位或说明，描述新的健康报告三层结构。
- [x] 6.2 更新 `docs/roadmap.zh-CN.md`，把健康报告重构标记为 0.2 体验整理项。
- [x] 6.3 保留本 change 的三张 UI 参考图在 `docs/images/`。

## 7. 验证

- [x] 7.1 运行 `npm run spec:validate -- refactor-tag-health-report-workflow`。
- [x] 7.2 运行 `npm test`。
- [x] 7.3 运行 `npm run build`。
- [x] 7.4 使用 `npm run local:install-dev` 安装到测试库。
- [ ] 7.5 在 Obsidian 中验证初始态、分析中态、结果态、规则 tab、可执行按钮和不可执行按钮边界。
