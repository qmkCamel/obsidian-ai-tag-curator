# 0.3 文件夹级安全批量预览验收记录

日期：2026-08-04

## 自动化验证

- `npm test`：通过，35 个测试文件、101 个测试全部通过。
- `npm run build`：通过，TypeScript 检查与生产构建成功。
- `npm run spec:validate -- add-safe-folder-batch-preview`：通过。
- `npm run spec:validate -- --all`：通过，6 个 change/spec 全部有效。
- `git diff --check`：通过。
- 自动化覆盖范围包括：范围前置与禁止截断、标签来源汇总、并发上限、立即取消与晚到结果丢弃、AI 失败本地同步、失败项重试、风险选择、内容/tags 冲突零写入、逐文件 CAS、补偿、固定 before/after 恢复目标、插件重载对账和跨重载整体回退。

## dev 安装验证

- 已运行 `npm run local:install-dev`。
- 安装目录：`/Users/edge/personal/edge-notes/.obsidian/plugins/ai-tag-curator-dev`。
- `main.js` 与 `styles.css` 的 SHA-256 和仓库构建产物一致。
- dev `manifest.json` 由安装脚本改写插件 id，因此与仓库原始 manifest 的校验值不同，属于预期行为。

## 真实 Obsidian 视觉与手动 smoke 边界

本次尝试通过 Computer Use 打开 Obsidian 测试库，系统返回：`Computer Use permissions are not granted`。

因此以下项目没有被声明为已验证，也没有保存伪造截图：

- 深色/浅色主题；
- 宽/窄窗口与长路径布局；
- 完整键盘操作；
- 真实 provider 下的取消计费提示、AI 失败与失败重试；
- 真实库中的内容漂移、应用、before/after 固定目标恢复和重载后回退。

自动化结果不能替代上述真实 Obsidian 视觉证明。待授予 Computer Use 权限或用户手动完成测试矩阵后，再勾选 OpenSpec 任务 10.6 与 10.7。
